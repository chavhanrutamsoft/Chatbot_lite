# Lightweight requirements for 512 MB memory (Render Free Tier)
# Removed heavy dependencies that are not needed at runtime

# Core dependencies
python-dotenv
qdrant-client
requests
numpy
gunicorn
flask
flask-cors
sentence-transformers

# Removed (only needed for ingestion, not runtime):
# python-docx - only needed for document extraction
# tqdm - only for progress bars
# rich - only for pretty printing
# watchdog - only for auto-ingestion
